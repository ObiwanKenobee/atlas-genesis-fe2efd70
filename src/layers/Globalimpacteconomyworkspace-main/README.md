
  # Global Impact Economy Workspace

  This is a code bundle for Global Impact Economy Workspace. The original project is available at https://www.figma.com/design/GplXticVHwiIywiXWB7mEG/Global-Impact-Economy-Workspace.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  