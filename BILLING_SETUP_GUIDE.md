["Stripe](https://stripe.com) and sign up for an account\n2. Navigate to the Dashboard\n3. Get your API keys from the Developers section\n\n### 2. Get API Keys\n1. Go to Developers > API keys\n2. Copy your **Publishable key** (starts with `pk_`)\n3. Copy your **Secret key** (starts with `sk_`)\n\n### 3. Create Webhook Endpoint\n1. Go to Developers > Webhooks\n2. Click \"Add endpoint", 3.0, "Set the endpoint URL to: `https://yourdomain.com/api/billing/webhook`\n4. Select the following events to listen to:\n   - `invoice.payment_succeeded`\n   - `invoice.payment_failed`\n   - `customer.subscription.created`\n   - `customer.subscription.updated`\n   - `customer.subscription.deleted`\n   - `payment_intent.succeeded`\n   - `payment_intent.payment_failed`\n   - `payment_intent.canceled`\n5. Copy the webhook signing secret (starts with `whsec_`)\n\n### 4. Create Billing Plans\n1. Go to Products > Products\n2. Create products for each billing plan:\n   - Starter Plan\n   - Professional Plan\n   - Enterprise Plan\n3. For each product, create pricing with:\n   - Recurring billing (monthly/yearly)\n   - Trial period (optional)\n   - Feature limits\n\n## Step 4: Run Database Migration\n\nExecute the SQL migration file to create the billing tables:\n\n```bash\n# Using psql\npsql -U your_username -d your_database -f database/migrations/add_billing_tables.sql\n\n# Or using your database client\n# Open database/migrations/add_billing_tables.sql and execute it\n```\n\n## Step 5: Seed Billing Plans\n\nRun the following SQL to seed the billing plans:\n\n```sql\n-- Insert billing plans\nINSERT INTO billing_plans (name, description, amount, interval, features, api_calls_limit, storage_limit, team_members_limit, created_at, updated_at) VALUES\n('Starter', 'Perfect for individuals and small teams', 2900, 'month', ARRAY['Basic marketplace access', 'Up to 100 API calls/month', '1GB storage', 'Email support'], 100, 1073741824, 1, NOW(), NOW()),\n('Professional', 'For growing businesses', 9900, 'month', ARRAY['Full marketplace access', 'Up to 1000 API calls/month', '10GB storage', 'Priority support', 'Advanced analytics'], 1000, 10737418240, 5, NOW(), NOW()),\n('Enterprise', 'For large organizations', 29900, 'month', ARRAY['Full marketplace access', 'Unlimited API calls', '100GB storage', '24/7 support', 'Custom integrations', 'Dedicated account manager'], 100000, 107374182400, 50, NOW(), NOW());\n```\n\n## Step 6: Test the Implementation\n\n### 1. Test Billing Dashboard\n1. Navigate to `/billing`\n2. Verify that the billing dashboard loads correctly\n3. Check that subscription information is displayed\n\n### 2. Test Invoices\n1. Navigate to `/invoices`\n2. Verify that invoices are listed\n3. Test invoice PDF download\n4. Test email invoice functionality\n\n### 3. Test Payment Methods\n1. Navigate to `/payment-methods`\n2. Test adding a new payment method\n3. Test setting default payment method\n4. Test deleting a payment method\n\n### 4. Test Webhooks\n1. Use Stripe CLI to test webhooks:\n```bash\nstripe listen --forward-to localhost:3001/api/billing/webhook\n```\n\n2. Trigger test events:\n```bash\nstripe trigger invoice.payment_succeeded\nstripe trigger invoice.payment_failed\nstripe trigger customer.subscription.created\n```\n\n## Step 7: Configure Production Settings\n\n### 1. Update CORS Settings\nIn `backend/.env`:\n```env\nCORS_ORIGIN=https://yourdomain.com\n```\n\n### 2. Configure Webhook URL\nUpdate your Stripe webhook endpoint to use your production URL:\n```\nhttps://yourdomain.com/api/billing/webhook\n```\n\n### 3. Set Up Monitoring\n- Configure logging for billing operations\n- Set up alerts for payment failures\n- Monitor webhook delivery status\n\n## Troubleshooting\n\n### Stripe SDK Not Found\nIf you see \"Cannot find module 'stripe'", "errors:\n```bash\n# Frontend\nnpm install stripe @types/stripe\n\n# Backend\ncd backend\nnpm install stripe @types/stripe --legacy-peer-deps\n```\n\n### Webhook Verification Failed\n- Ensure `STRIPE_WEBHOOK_SECRET` is set correctly\n- Check that the webhook endpoint is accessible\n- Verify webhook events are being sent from Stripe\n\n### Database Errors\n- Ensure database migration was run successfully\n- Check database connection settings\n- Verify table permissions\n\n### Payment Processing Errors\n- Check Stripe API keys are correct\n- Verify payment method is valid\n- Check for sufficient funds\n\n## Security Considerations\n\n1. **Never commit API keys** to version control\n2. **Use environment variables** for all sensitive data\n3. **Enable webhook signature verification** to prevent fraud\n4. **Implement rate limiting** on billing endpoints\n5. **Log all billing operations** for audit trails\n6. **Use HTTPS** for all billing-related requests\n\n## Support\n\nFor issues or questions:\n- Check the [Stripe Documentation](https://stripe.com/docs)\n- Review the [Implementation Guide](./PHASE_6_BILLING_SUBSCRIPTIONS_IMPLEMENTATION.md)\n- Contact support for platform-specific issues\n\n## Next Steps\n\nAfter completing the setup:\n1. Test the complete billing flow\n2. Set up automated testing\n3. Configure monitoring and alerts\n4. Document your billing processes\n5. Train your team on the billing system"]